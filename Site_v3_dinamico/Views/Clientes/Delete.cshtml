@model Site_v3_dinamico.Models.Cliente

@{
    ViewData["Title"] = "Delete";
}

<section class="section my-services" data-section="section5">
    <div class="container">
        <div class="section-heading">
            <h2>Apagar conta de cliente</h2>
            <div class="line-dec"></div>
            <h3 style="color: white;">Tem a certeza de que pretende apagar a conta deste cliente?</h3>
        </div>
        <div class="table-responsive">
            <table class="table table-dark">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Nome)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Telemóvel)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Email)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>

                    <tr>
                        <td>
                            @Html.DisplayFor(model => model.Nome)
                        </td>
                        <td>
                            @Html.DisplayFor(model => model.Telemóvel)
                        </td>
                        <td>
                            @Html.DisplayFor(model => model.Email)
                        </td>
                        <td>
                    </tr>
            </table>
        </div>
        <form asp-action="Delete">
            <input type="hidden" asp-for="ClienteId" />
            <input type="submit" value="Apagar" class="btn btn-danger" /> |
            <a class="btn btn-primary" asp-action="Index">Cancelar</a>
        </form>
    </div>
</section>